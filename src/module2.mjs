import default_value ,{b,c} from "./module1.mjs";

// by default it gets the default value from module1.mjs (any name can be used)
// to export other non-default values use braces and use same name
console.log(default_value)
console.log(b)
console.log(c)